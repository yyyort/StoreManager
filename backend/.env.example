# ============================================
# ENVIRONMENT VARIABLES FOR PRODUCTION
# ============================================
# 
# For DEVELOPMENT: Use .NET User Secrets instead
#   Run: dotnet user-secrets set "Jwt:Key" "your-key-here"
#   See: docs/USER_SECRETS.md
#
# For PRODUCTION: Set these as environment variables
#   - Azure App Service: Application Settings
#   - Docker: Environment variables or secrets
#   - Kubernetes: ConfigMaps and Secrets
#   - Linux/Mac: export KEY=value
#   - Windows: setx KEY value
#
# ============================================

# JWT Configuration
# Generate a secure random key with at least 32 characters
# You can use: openssl rand -base64 64
JWT__KEY=YourProductionSecretKeyThatIsAtLeast32CharactersLong!ChangeThisInProduction!
JWT__ISSUER=StoreManagerAPI
JWT__AUDIENCE=StoreManagerClient
JWT__EXPIRATIONDAYS=7

# Database Connection
# Replace with your production database connection string
CONNECTIONSTRINGS__DEFAULTCONNECTION=Server=your-server;Database=StoreManager;User Id=your-user;Password=your-password;TrustServerCertificate=True;MultipleActiveResultSets=true

# ASP.NET Core Environment
ASPNETCORE_ENVIRONMENT=Production
ASPNETCORE_URLS=https://+:443;http://+:80

# CORS Origins (comma-separated list of allowed origins)
# Example: https://yourdomain.com,https://www.yourdomain.com
CORS__ALLOWEDORIGINS=https://yourdomain.com

# ============================================
# NOTES:
# ============================================
# 1. NEVER commit this file with real secrets
# 2. Use double underscores (__) for nested configuration
# 3. Generate strong keys: openssl rand -base64 64
# 4. Rotate keys periodically
# 5. Use different keys per environment
# ============================================
